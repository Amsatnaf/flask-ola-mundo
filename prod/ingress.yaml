apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: flask-web
  annotations:
    # cert-manager.io/cluster-issuer: "letsencrypt-prod"  # opcional para TLS
spec:
  ingressClassName: traefik
  rules:
    - host: flask.prod.129.213.28.76.sslip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: flask-web
                port:
                  number: 80
# tls:
#   - hosts:
#       - flask.prod.129.213.28.76.sslip.io
#     secretName: flask-prod-tls
